#!/usr/bin/env python

import sys
from pkg_resources import load_entry_point
from snakebasket.main import main

# Add the rinstall commands module
sys.modules['pip.commands.rinstall'] = rinstall


# Start pip as normal
if __name__ == '__main__':
    sys.exit(
        main(sys.argv[:1])
    )
